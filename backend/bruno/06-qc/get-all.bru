meta {
  name: Get All QC Logs
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/qc
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("should return 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
